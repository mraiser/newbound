{"data":{"attachmentkeynames":["rs"],"desc":"Update the platform's flowlang/ndata crate versions. Step 0 runs in-process: rewrite the pins in every manifest in lockstep (root, newbound_core, cmd, all FFI crate roots; each line's =-pin style preserved) and set the in-memory hotswap_paused gate. Everything else runs through the NEW code via a detached command-line runner (setsid, logged): (1) cargo build feature-matched to this instance, (2) newbound rebuild, (3) newbound recompile, (4) final cargo build - restart verdict by host-binary hash before/after. Refuses an ndata bump without a flowlang bump (ndata rides flowlang releases; the hotswap ABI-contract guard keys on the flowlang version) and refuses to stack runs. Poll dev.dev.update_crates_status for progress and the verdict; equal-to-current versions are allowed as a repair re-run. Callable on a peer via /peer/remote/uuid/dev/update_crates.","groups":"admin","import":"use flowlang::datastore::DataStore;\nuse ndata::dataarray::DataArray;\nuse crate::dev::dev::crate_versions::manifest_paths;\nuse crate::dev::dev::crate_versions::read_dep_version;\nuse crate::dev::dev::crate_versions::rewrite_dep_version;\nuse crate::dev::dev::compile::build_compile_command;\nuse std::fs;\nuse std::process::Command;\nuse std::process::Stdio;\n","params":[{"desc":"The flowlang crate version to pin (e.g. 0.3.35). May equal the current version to re-run the pipeline as a repair.","name":"flowlang","type":"String"},{"desc":"The ndata crate version to pin (e.g. 0.3.18). An ndata change without a flowlang change is refused: ndata rides flowlang releases, and the hotswap ABI-contract guard keys on the flowlang version.","name":"ndata","type":"String"}],"returntype":"FLAT","type":"rust"},"id":"mzhpqp1a052c4c270sc","readers":["admin"],"time":1788095089723,"username":"system","writers":[]}
{"data":{"list":[{"author":"installer","facet":"html","label":"install sync","new":"<button class=\"nb-card\">\n  <span class=\"c-name\"></span>\n  <span class=\"c-desc\"></span>\n  <span class=\"c-dots\" aria-hidden=\"false\"></span>\n</button>\n","old":"","patch_id":"p1","time":1785011188461},{"author":"installer","facet":"css","label":"install sync","new":".nb-card {\n  display: flex;\n  flex-direction: column;\n  align-items: flex-start;\n  gap: 4px;\n  width: 100%;\n  text-align: left;\n  padding: 14px 15px 12px;\n  border-radius: 10px;\n  background: var(--paper);\n  color: var(--ink);\n  border: 1px solid var(--paper-edge);\n  box-shadow: 0 2px 0 rgba(0, 0, 0, 0.35);\n  transition: transform 0.12s ease, box-shadow 0.12s ease;\n}\n.nb-card:hover {\n  transform: translateY(-3px);\n  box-shadow: 0 8px 18px rgba(0, 0, 0, 0.4);\n}\n.nb-card .c-name {\n  font-family: var(--mono);\n  font-size: 13.5px;\n  font-weight: 600;\n}\n.nb-card .c-desc {\n  font-size: 11.5px;\n  color: var(--ink-dim);\n  line-height: 1.4;\n  min-height: 16px;\n}\n.nb-card .c-dots {\n  display: flex;\n  gap: 5px;\n  margin-top: 6px;\n}\n.nb-card .c-dot {\n  width: 8px;\n  height: 8px;\n  border-radius: 50%;\n  background: #ddd6c8;\n}\n.nb-card .c-dot.on-html { background: var(--f-html); }\n.nb-card .c-dot.on-css { background: var(--f-css); }\n.nb-card .c-dot.on-js { background: var(--f-js); }\n.nb-card .c-dot.on-data { background: var(--f-data); }\n.nb-card .c-dot.on-three { background: var(--f-3d); }\n.nb-card .c-dot.on-cmd { background: var(--f-cmd); }\n","old":"","patch_id":"p2","time":1785011188468},{"author":"installer","facet":"js","label":"install sync","new":"// card — the paper control-card: name, description, and six facet dots\n// showing which facets the control carries. Used by the shelf's fan.\n\nimport { FACETS } from \"../../assets/facets.js\";\n\nexport function init(host, { name, onOpen }) {\n  const root = host.querySelector(\".nb-card\");\n  root.querySelector(\".c-name\").textContent = name;\n  root.addEventListener(\"click\", onOpen);\n\n  const dotsEl = root.querySelector(\".c-dots\");\n  for (const facet of FACETS) {\n    const dot = document.createElement(\"span\");\n    dot.className = \"c-dot\";\n    dot.dataset.facet = facet;\n    dot.setAttribute(\"role\", \"img\");\n    dot.setAttribute(\"aria-label\", `${facet}: unknown`);\n    dotsEl.appendChild(dot);\n  }\n\n  return {\n    /** Fill in description + facet dots once the control record is loaded. */\n    setRecord(record) {\n      root.querySelector(\".c-desc\").textContent = record?.desc ?? \"\";\n      for (const dot of dotsEl.children) {\n        const facet = dot.dataset.facet;\n        const present = record?.[facet] !== undefined\n          && (facet !== \"cmd\" || (record.cmd?.length ?? 0) > 0);\n        dot.classList.toggle(`on-${facet}`, present);\n        dot.setAttribute(\"aria-label\", `${facet}: ${record ? (present ? \"present\" : \"absent\") : \"unknown\"}`);\n      }\n    },\n  };\n}\n","old":"","patch_id":"p3","time":1785011188475},{"author":"installer","facet":"css","label":"install sync","new":".nb-card {\n  display: flex;\n  flex-direction: column;\n  align-items: flex-start;\n  gap: 4px;\n  width: 100%;\n  text-align: left;\n  padding: 14px 15px 12px;\n  border-radius: 10px;\n  background: var(--paper);\n  color: var(--ink);\n  border: 1px solid var(--paper-edge);\n  box-shadow: 0 2px 0 rgba(0, 0, 0, 0.35);\n  transition: transform 0.12s ease, box-shadow 0.12s ease;\n}\n.nb-card:hover {\n  transform: translateY(-3px);\n  box-shadow: 0 8px 18px rgba(0, 0, 0, 0.4);\n}\n.nb-card .c-name {\n  font-family: var(--mono);\n  font-size: 13.5px;\n  font-weight: 600;\n}\n.nb-card .c-desc {\n  font-size: 11.5px;\n  color: var(--ink-dim);\n  line-height: 1.4;\n  min-height: 16px;\n}\n.nb-card .c-dots {\n  display: flex;\n  gap: 5px;\n  margin-top: 6px;\n}\n.nb-card .c-dot {\n  width: 8px;\n  height: 8px;\n  border-radius: 50%;\n  background: #ddd6c8;\n}\n.nb-card .c-dot.on-html { background: var(--f-html); }\n.nb-card .c-dot.on-css { background: var(--f-css); }\n.nb-card .c-dot.on-js { background: var(--f-js); }\n.nb-card .c-dot.on-data { background: var(--f-data); }\n.nb-card .c-dot.on-scene { background: var(--f-3d); }\n.nb-card .c-dot.on-cmd { background: var(--f-cmd); }\n","old":".nb-card {\n  display: flex;\n  flex-direction: column;\n  align-items: flex-start;\n  gap: 4px;\n  width: 100%;\n  text-align: left;\n  padding: 14px 15px 12px;\n  border-radius: 10px;\n  background: var(--paper);\n  color: var(--ink);\n  border: 1px solid var(--paper-edge);\n  box-shadow: 0 2px 0 rgba(0, 0, 0, 0.35);\n  transition: transform 0.12s ease, box-shadow 0.12s ease;\n}\n.nb-card:hover {\n  transform: translateY(-3px);\n  box-shadow: 0 8px 18px rgba(0, 0, 0, 0.4);\n}\n.nb-card .c-name {\n  font-family: var(--mono);\n  font-size: 13.5px;\n  font-weight: 600;\n}\n.nb-card .c-desc {\n  font-size: 11.5px;\n  color: var(--ink-dim);\n  line-height: 1.4;\n  min-height: 16px;\n}\n.nb-card .c-dots {\n  display: flex;\n  gap: 5px;\n  margin-top: 6px;\n}\n.nb-card .c-dot {\n  width: 8px;\n  height: 8px;\n  border-radius: 50%;\n  background: #ddd6c8;\n}\n.nb-card .c-dot.on-html { background: var(--f-html); }\n.nb-card .c-dot.on-css { background: var(--f-css); }\n.nb-card .c-dot.on-js { background: var(--f-js); }\n.nb-card .c-dot.on-data { background: var(--f-data); }\n.nb-card .c-dot.on-three { background: var(--f-3d); }\n.nb-card .c-dot.on-cmd { background: var(--f-cmd); }\n","patch_id":"p4","time":1785152140203},{"author":"installer","facet":"js","label":"install sync","new":"// card — the paper control-card: name, description, and six facet dots\n// showing which facets the control carries. Used by the shelf's fan.\n\nimport { FACETS } from \"../../assets/facets.js\";\n\nexport function init(host, { name, onOpen }) {\n  const root = host.querySelector(\".nb-card\");\n  root.querySelector(\".c-name\").textContent = name;\n  root.addEventListener(\"click\", onOpen);\n\n  const dotsEl = root.querySelector(\".c-dots\");\n  for (const facet of FACETS) {\n    const dot = document.createElement(\"span\");\n    dot.className = \"c-dot\";\n    dot.dataset.facet = facet;\n    dot.setAttribute(\"role\", \"img\");\n    dot.setAttribute(\"aria-label\", `${facet}: unknown`);\n    dotsEl.appendChild(dot);\n  }\n\n  return {\n    /** Fill in description + facet dots once the control record is loaded. */\n    setRecord(record) {\n      root.querySelector(\".c-desc\").textContent = record?.desc ?? \"\";\n      for (const dot of dotsEl.children) {\n        const facet = dot.dataset.facet;\n        // The 3d dot lights on the scene facet OR the legacy three facet\n        // (scene-facet-design SC-Q2).\n        const present = facet === \"scene\"\n          ? record?.scene !== undefined || record?.three !== undefined\n          : record?.[facet] !== undefined\n            && (facet !== \"cmd\" || (record.cmd?.length ?? 0) > 0);\n        dot.classList.toggle(`on-${facet}`, present);\n        dot.setAttribute(\"aria-label\", `${facet}: ${record ? (present ? \"present\" : \"absent\") : \"unknown\"}`);\n      }\n    },\n  };\n}\n","old":"// card — the paper control-card: name, description, and six facet dots\n// showing which facets the control carries. Used by the shelf's fan.\n\nimport { FACETS } from \"../../assets/facets.js\";\n\nexport function init(host, { name, onOpen }) {\n  const root = host.querySelector(\".nb-card\");\n  root.querySelector(\".c-name\").textContent = name;\n  root.addEventListener(\"click\", onOpen);\n\n  const dotsEl = root.querySelector(\".c-dots\");\n  for (const facet of FACETS) {\n    const dot = document.createElement(\"span\");\n    dot.className = \"c-dot\";\n    dot.dataset.facet = facet;\n    dot.setAttribute(\"role\", \"img\");\n    dot.setAttribute(\"aria-label\", `${facet}: unknown`);\n    dotsEl.appendChild(dot);\n  }\n\n  return {\n    /** Fill in description + facet dots once the control record is loaded. */\n    setRecord(record) {\n      root.querySelector(\".c-desc\").textContent = record?.desc ?? \"\";\n      for (const dot of dotsEl.children) {\n        const facet = dot.dataset.facet;\n        const present = record?.[facet] !== undefined\n          && (facet !== \"cmd\" || (record.cmd?.length ?? 0) > 0);\n        dot.classList.toggle(`on-${facet}`, present);\n        dot.setAttribute(\"aria-label\", `${facet}: ${record ? (present ? \"present\" : \"absent\") : \"unknown\"}`);\n      }\n    },\n  };\n}\n","patch_id":"p5","time":1785152140206},{"author":"claude","facet":"move","label":"moved from bench to dev","new":"dev","old":"bench","patch_id":"p6","time":1785372127261},{"author":"claude","facet":"html","label":"groups-ui","new":"  <span class=\"c-name\"></span>\n  <span class=\"c-gate\" aria-hidden=\"false\"></span>\n  <span class=\"c-desc\"></span>","old":"  <span class=\"c-name\"></span>\n  <span class=\"c-desc\"></span>","patch_id":"p7","time":1786308808075},{"author":"claude","facet":"js","label":"groups-ui","new":"    setRecord(record) {\n      root.querySelector(\".c-desc\").textContent = record?.desc ?? \"\";\n      // The access badge: the security-group string verbatim; open\n      // (anonymous) shows nothing; no groups = the platform default,\n      // admin-only.\n      const gateEl = root.querySelector(\".c-gate\");\n      const g = (record?.groups ?? \"\").trim();\n      const open = g.split(\",\").map((s) => s.trim()).includes(\"anonymous\");\n      gateEl.textContent = record ? (g ? (open ? \"\" : `⛨ ${g}`) : \"⛨ admin\") : \"\";\n      gateEl.title = g ? `security groups: ${g}` : \"admin only — no groups granted\";","old":"    setRecord(record) {\n      root.querySelector(\".c-desc\").textContent = record?.desc ?? \"\";","patch_id":"p8","time":1786308808104},{"author":"claude","facet":"css","label":"groups-ui","new":".nb-card .c-desc {\n  font-size: 11.5px;\n  color: var(--ink-dim);\n  line-height: 1.4;\n  min-height: 16px;\n}\n.nb-card .c-gate { font: 10px var(--mono); color: var(--ink-dim); }\n.nb-card .c-gate:empty { display: none; }","old":".nb-card .c-desc {\n  font-size: 11.5px;\n  color: var(--ink-dim);\n  line-height: 1.4;\n  min-height: 16px;\n}","patch_id":"p9","time":1786308808132},{"author":"claude","facet":"js","label":"stock-dialect conversion (collapse the bench graft, leaf pass)","new":"// card — the paper control-card: name, description, and six facet dots\n// showing which facets the control carries. Used by the shelf's fan.\n//\n// DATA: {name, onOpen}. api: setRecord(record).\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nvar root = ME.querySelector(\".nb-card\");\nvar dotsEl = root.querySelector(\".c-dots\");\nroot.querySelector(\".c-name\").textContent = ME.DATA.name;\nroot.addEventListener(\"click\", ME.DATA.onOpen);\n\n// the dots wait on the facets module; setRecord queues behind them\nvar dotsReady = requireModule(\"facets\", \"card\").then(function (m) {\n  for (const facet of m.FACETS) {\n    const dot = document.createElement(\"span\");\n    dot.className = \"c-dot\";\n    dot.dataset.facet = facet;\n    dot.setAttribute(\"role\", \"img\");\n    dot.setAttribute(\"aria-label\", `${facet}: unknown`);\n    dotsEl.appendChild(dot);\n  }\n});\n\n/** Fill in description + facet dots once the control record is loaded. */\nme.setRecord = function (record) {\n  dotsReady.then(function () {\n    root.querySelector(\".c-desc\").textContent = record?.desc ?? \"\";\n    // The access badge: the security-group string verbatim; open\n    // (anonymous) shows nothing; no groups = the platform default,\n    // admin-only.\n    const gateEl = root.querySelector(\".c-gate\");\n    const g = (record?.groups ?? \"\").trim();\n    const open = g.split(\",\").map((s) => s.trim()).includes(\"anonymous\");\n    gateEl.textContent = record ? (g ? (open ? \"\" : `⛨ ${g}`) : \"⛨ admin\") : \"\";\n    gateEl.title = g ? `security groups: ${g}` : \"admin only — no groups granted\";\n    for (const dot of dotsEl.children) {\n      const facet = dot.dataset.facet;\n      // The 3d dot lights on the scene facet OR the legacy three facet\n      // (scene-facet-design SC-Q2).\n      const present = facet === \"scene\"\n        ? record?.scene !== undefined || record?.three !== undefined\n        : record?.[facet] !== undefined\n          && (facet !== \"cmd\" || (record.cmd?.length ?? 0) > 0);\n      dot.classList.toggle(`on-${facet}`, present);\n      dot.setAttribute(\"aria-label\", `${facet}: ${record ? (present ? \"present\" : \"absent\") : \"unknown\"}`);\n    }\n  });\n};\n","old":"// card — the paper control-card: name, description, and six facet dots\n// showing which facets the control carries. Used by the shelf's fan.\n\nimport { FACETS } from \"../../assets/facets.js\";\n\nexport function init(host, { name, onOpen }) {\n  const root = host.querySelector(\".nb-card\");\n  root.querySelector(\".c-name\").textContent = name;\n  root.addEventListener(\"click\", onOpen);\n\n  const dotsEl = root.querySelector(\".c-dots\");\n  for (const facet of FACETS) {\n    const dot = document.createElement(\"span\");\n    dot.className = \"c-dot\";\n    dot.dataset.facet = facet;\n    dot.setAttribute(\"role\", \"img\");\n    dot.setAttribute(\"aria-label\", `${facet}: unknown`);\n    dotsEl.appendChild(dot);\n  }\n\n  return {\n    /** Fill in description + facet dots once the control record is loaded. */\n    setRecord(record) {\n      root.querySelector(\".c-desc\").textContent = record?.desc ?? \"\";\n      // The access badge: the security-group string verbatim; open\n      // (anonymous) shows nothing; no groups = the platform default,\n      // admin-only.\n      const gateEl = root.querySelector(\".c-gate\");\n      const g = (record?.groups ?? \"\").trim();\n      const open = g.split(\",\").map((s) => s.trim()).includes(\"anonymous\");\n      gateEl.textContent = record ? (g ? (open ? \"\" : `⛨ ${g}`) : \"⛨ admin\") : \"\";\n      gateEl.title = g ? `security groups: ${g}` : \"admin only — no groups granted\";\n      for (const dot of dotsEl.children) {\n        const facet = dot.dataset.facet;\n        // The 3d dot lights on the scene facet OR the legacy three facet\n        // (scene-facet-design SC-Q2).\n        const present = facet === \"scene\"\n          ? record?.scene !== undefined || record?.three !== undefined\n          : record?.[facet] !== undefined\n            && (facet !== \"cmd\" || (record.cmd?.length ?? 0) > 0);\n        dot.classList.toggle(`on-${facet}`, present);\n        dot.setAttribute(\"aria-label\", `${facet}: ${record ? (present ? \"present\" : \"absent\") : \"unknown\"}`);\n      }\n    },\n  };\n}\n","patch_id":"p10","time":1786399355947},{"author":"claude","facet":"js","label":"off the store module: api.js primitives, saves-ON posture removed","new":"// card — the paper control-card: name, description, and six facet dots\n// showing which facets the control carries. Used by the shelf's fan.\n//\n// DATA: {name, onOpen}. api: setRecord(record).\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nvar root = ME.querySelector(\".nb-card\");\nvar dotsEl = root.querySelector(\".c-dots\");\nroot.querySelector(\".c-name\").textContent = ME.DATA.name;\nroot.addEventListener(\"click\", ME.DATA.onOpen);\n\n// the dots wait on the facets library (a child div, ready before this\n// control's ready); setRecord queues behind them\nvar dotsReady = new Promise(function (res) { me.ready = res; }).then(function () {\n  const { FACETS } = window.NB_FACETS;\n  for (const facet of FACETS) {\n    const dot = document.createElement(\"span\");\n    dot.className = \"c-dot\";\n    dot.dataset.facet = facet;\n    dot.setAttribute(\"role\", \"img\");\n    dot.setAttribute(\"aria-label\", `${facet}: unknown`);\n    dotsEl.appendChild(dot);\n  }\n});\n\n/** Fill in description + facet dots once the control record is loaded. */\nme.setRecord = function (record) {\n  dotsReady.then(function () {\n    root.querySelector(\".c-desc\").textContent = record?.desc ?? \"\";\n    // The access badge: the security-group string verbatim; open\n    // (anonymous) shows nothing; no groups = the platform default,\n    // admin-only.\n    const gateEl = root.querySelector(\".c-gate\");\n    const g = (record?.groups ?? \"\").trim();\n    const open = g.split(\",\").map((s) => s.trim()).includes(\"anonymous\");\n    gateEl.textContent = record ? (g ? (open ? \"\" : `⛨ ${g}`) : \"⛨ admin\") : \"\";\n    gateEl.title = g ? `security groups: ${g}` : \"admin only — no groups granted\";\n    for (const dot of dotsEl.children) {\n      const facet = dot.dataset.facet;\n      // The 3d dot lights on the scene facet OR the legacy three facet\n      // (scene-facet-design SC-Q2).\n      const present = facet === \"scene\"\n        ? record?.scene !== undefined || record?.three !== undefined\n        : record?.[facet] !== undefined\n          && (facet !== \"cmd\" || (record.cmd?.length ?? 0) > 0);\n      dot.classList.toggle(`on-${facet}`, present);\n      dot.setAttribute(\"aria-label\", `${facet}: ${record ? (present ? \"present\" : \"absent\") : \"unknown\"}`);\n    }\n  });\n};\n","old":"// card — the paper control-card: name, description, and six facet dots\n// showing which facets the control carries. Used by the shelf's fan.\n//\n// DATA: {name, onOpen}. api: setRecord(record).\n\nvar me = this;\nvar ME = document.getElementById(me.UUID);\n\nvar root = ME.querySelector(\".nb-card\");\nvar dotsEl = root.querySelector(\".c-dots\");\nroot.querySelector(\".c-name\").textContent = ME.DATA.name;\nroot.addEventListener(\"click\", ME.DATA.onOpen);\n\n// the dots wait on the facets module; setRecord queues behind them\nvar dotsReady = requireModule(\"facets\", \"card\").then(function (m) {\n  for (const facet of m.FACETS) {\n    const dot = document.createElement(\"span\");\n    dot.className = \"c-dot\";\n    dot.dataset.facet = facet;\n    dot.setAttribute(\"role\", \"img\");\n    dot.setAttribute(\"aria-label\", `${facet}: unknown`);\n    dotsEl.appendChild(dot);\n  }\n});\n\n/** Fill in description + facet dots once the control record is loaded. */\nme.setRecord = function (record) {\n  dotsReady.then(function () {\n    root.querySelector(\".c-desc\").textContent = record?.desc ?? \"\";\n    // The access badge: the security-group string verbatim; open\n    // (anonymous) shows nothing; no groups = the platform default,\n    // admin-only.\n    const gateEl = root.querySelector(\".c-gate\");\n    const g = (record?.groups ?? \"\").trim();\n    const open = g.split(\",\").map((s) => s.trim()).includes(\"anonymous\");\n    gateEl.textContent = record ? (g ? (open ? \"\" : `⛨ ${g}`) : \"⛨ admin\") : \"\";\n    gateEl.title = g ? `security groups: ${g}` : \"admin only — no groups granted\";\n    for (const dot of dotsEl.children) {\n      const facet = dot.dataset.facet;\n      // The 3d dot lights on the scene facet OR the legacy three facet\n      // (scene-facet-design SC-Q2).\n      const present = facet === \"scene\"\n        ? record?.scene !== undefined || record?.three !== undefined\n        : record?.[facet] !== undefined\n          && (facet !== \"cmd\" || (record.cmd?.length ?? 0) > 0);\n      dot.classList.toggle(`on-${facet}`, present);\n      dot.setAttribute(\"aria-label\", `${facet}: ${record ? (present ? \"present\" : \"absent\") : \"unknown\"}`);\n    }\n  });\n};\n","patch_id":"p11","time":1786403189361},{"author":"claude","facet":"html","label":"off the store module: api.js primitives, saves-ON posture removed","new":"<button class=\"nb-card\">\n  <span class=\"c-name\"></span>\n  <span class=\"c-gate\" aria-hidden=\"false\"></span>\n  <span class=\"c-desc\"></span>\n  <span class=\"c-dots\" aria-hidden=\"false\"></span>\n</button>\n<div class=\"data-control\" data-control=\"dev:facets\" hidden></div>\n","old":"<button class=\"nb-card\">\n  <span class=\"c-name\"></span>\n  <span class=\"c-gate\" aria-hidden=\"false\"></span>\n  <span class=\"c-desc\"></span>\n  <span class=\"c-dots\" aria-hidden=\"false\"></span>\n</button>\n","patch_id":"p12","time":1786403189652}]},"id":"uzypnk19f9af532cav3_patches","readers":[],"time":1786403189652,"username":"system","writers":[]}
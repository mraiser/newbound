{"data":{"attachmentkeynames":["rs"],"desc":"\"This is bad\" — abandon a feature branch and fall back to the repo's default branch (local master, else main). Guarded destructive workflow: refuses to delete the default branch or a nonexistent one; checkout default → branch -D the feature branch → optionally reset --hard HEAD when discard=true. discard defaults false, so a bare call only leaves the branch and deletes the label, never touches the working tree; the mutating-command confirm gate is the \"are you sure?\" by mechanism. Returns {status, msg, abandoned, now_on, discarded, steps}.","import":"use ndata::dataarray::DataArray;","params":[{"name":"repo","type":"String"},{"name":"branch","type":"String"},{"name":"discard","type":"Boolean"}],"returntype":"FLAT","type":"rust"},"id":"hgoiwu1a05cfe25f6g11c","readers":[],"time":1788267099232,"username":"system","writers":[]}